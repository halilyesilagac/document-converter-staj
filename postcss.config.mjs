// PostCSS konfigurasyonu
// Tailwind'in CSS donusumlerini buradan aktif ediyorum.
const config = {
  plugins: {
    // Tailwind CSS v4 icin resmi PostCSS eklentisi
    "@tailwindcss/postcss": {},
  },
};

export default config;
